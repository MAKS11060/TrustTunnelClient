name: Deploy Android library

on:
  workflow_dispatch:

jobs:
  deploy-android:
    name: Deploy library / Android
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Python 3.13+
        run: |
          python3 --version
          python3 -m pip install --upgrade pip

      - name: Install CMake 3.24+
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build

      - name: Install LLVM 17+
        run: |
          sudo apt-get install -y llvm clang libc++-dev

      - name: Install Conan 2.0.5+
        run: |
          python3 -m pip install 'conan>=2.0.5'

      - name: Install Rust 1.85+
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --default-toolchain 1.85 -y
          source $HOME/.cargo/env

      - name: Configure Conan remotes and profiles
        run: |
          conan remote add conancenter https://center.conan.io --force
          conan profile detect --force

      - name: Bootstrap Conan dependencies
        run: |
          python3 scripts/bootstrap_conan_deps.py
      - name: Build Android library
        run: |
          cd platform/android
          touch local.properties
          CMAKE_DIR=$(dirname $(dirname $(which cmake)))
          echo "cmake.dir=$CMAKE_DIR" >> local.properties

          ./gradlew :lib:assembleRelease

      - name: Publish to GitHub Packages
        env:
          USERNAME: ${{ github.actor }}
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cd platform/android
          ./gradlew :lib:publishReleasePublicationToGitHubPackagesRepository
# jobs:
#   deploy-android:
#     name: Deploy library / Android
#     # if: github.repository == 'AdGuardSoftwareLimited/trusttunnel-client'

#     environment: android-gh-packages
#     runs-on:
#       - ephemeral
#       - ephemeral-linux
#       - ${{ vars.BUILD_IMAGE_VERSION_CORELIBS_BUILDER }}
#       - ephemeral-limit-memory-8192
#       - ephemeral-limit-cpu-6
#     container:
#       image: adguard/core-libs:2.10
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v6

#       - name: Configure Conan
#         run: |
#           python3 -m pip install conan --upgrade
#           conan remote add --index 0 art ${{ vars.CONAN_REPO_URL }} || true

#       - name: Build Android library
#         run: |
#           cd platform/android
#           touch local.properties
#           CMAKE_DIR=$(dirname $(dirname $(which cmake)))
#           echo cmake.dir=$CMAKE_DIR >> local.properties
#           ./gradlew :lib:assembleRelease

#       - name: Publish to GitHub Packages
#         env:
#           USERNAME: github
#           TOKEN: ${{ secrets.PUBLIC_REPO_GPR_KEY }}
#         run: |
#           cd platform/android
#           ./gradlew :lib:publishReleasePublicationToGitHubPackagesRepository
